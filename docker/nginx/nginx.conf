# ══════════════════════════════════════════════════════════════════════════
# VisãoOS — Nginx Reverse Proxy Config
# Substitua seusite.com.br pelo seu domínio real
# ══════════════════════════════════════════════════════════════════════════

# ── n8n ──────────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name n8n.seusite.com.br;
    ssl_certificate     /etc/letsencrypt/live/n8n.seusite.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/n8n.seusite.com.br/privkey.pem;

    location / {
        proxy_pass http://n8n:5678;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        client_max_body_size 50M;
    }
}

# ── Metabase ──────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name metabase.seusite.com.br;
    ssl_certificate     /etc/letsencrypt/live/metabase.seusite.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/metabase.seusite.com.br/privkey.pem;

    location / {
        proxy_pass http://metabase:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}

# ── Nextcloud ─────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name cloud.seusite.com.br;
    ssl_certificate     /etc/letsencrypt/live/cloud.seusite.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cloud.seusite.com.br/privkey.pem;

    client_max_body_size 512M;

    location / {
        proxy_pass http://nextcloud:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        add_header Strict-Transport-Security "max-age=15768000; includeSubDomains; preload" always;
    }
}

# ── Mautic ────────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name mautic.seusite.com.br;
    ssl_certificate     /etc/letsencrypt/live/mautic.seusite.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/mautic.seusite.com.br/privkey.pem;

    location / {
        proxy_pass http://mautic:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ── Jenkins ───────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name ci.seusite.com.br;
    ssl_certificate     /etc/letsencrypt/live/ci.seusite.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ci.seusite.com.br/privkey.pem;

    location / {
        proxy_pass http://jenkins:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}

# Redireciona HTTP → HTTPS
server {
    listen 80;
    server_name n8n.seusite.com.br metabase.seusite.com.br cloud.seusite.com.br mautic.seusite.com.br ci.seusite.com.br;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://$host$request_uri; }
}
